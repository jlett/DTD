using UnityEngine;
using System.Collections;

public class LoadingScreen : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	public void DisplayLoadingScreen(string levelToLoad) { //simply overlay a texture for the scene to load. make sure to disable key inputs during this time

	}
}
